// Helper library for material design that contains UI widgets
import 'package:flutter/material.dart';

import 'src/app.dart';

// Main function that runs when app starts
void main() {
  // Runs everything that is inside it
  runApp(App());
}
